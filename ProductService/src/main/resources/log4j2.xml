<?xml version="1.0" encoding="UTF-8"?>
<Configuration packages="com.products.productservice.logging">
    <Appenders>

        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{HH:mm:ss} %-5level %logger{36} - %msg%n" />
        </Console>

        <CloudWatchAppender name="CloudWatch"
                            logGroupName="/examenU1/producto-log-group"
                            logStreamName="microservicio-productos"
                            endpoint="http://localstack-mainU1:4566">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} %-5level - %msg%n" />
        </CloudWatchAppender>

    </Appenders>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="Console" />
            <AppenderRef ref="CloudWatch" />
        </Root>
    </Loggers>
</Configuration>